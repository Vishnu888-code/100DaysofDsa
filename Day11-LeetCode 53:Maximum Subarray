class Solution {
public:
    int maxSubArray(vector<int>& nums) {
        int cur = 0, ans = nums[0];

        for (int x : nums) {
            cur = max(x, cur + x);
            ans = max(ans, cur);
        }

        return ans;
    }
};
